"""Purpose: package root for the Financial Agentic application's data pipeline. Declares the
version used to stamp published artifacts so a Silver output can always be traced back to the
code that produced it. Deliberately imports nothing, keeping the package cheap to import from
the CLI and from tests.
"""

__version__ = "0.1.0"
